# משימה: שדרוג אתר PROPEL — מיצוב, אמינות, SEO טכני וביצועים

> קרא את המסמך **במלואו** לפני שאתה נוגע בקובץ אחד.
> אתה עובד בשלבים. כל שלב נעצר לאישור שלי לפני מעבר לשלב הבא.

---

## 1. הקשר

**הפרויקט:** propel.co.il — אתר הסוכנות של PROPEL (בניית אתרים, אוטומציה עסקית, SEO).
**סטאק:** Next.js (App Router) + TypeScript + Tailwind, דו-לשוני `/he` + `/en`, מאוחסן ב-Netlify.
**קהל יעד:** בעלי עסקים קטנים-בינוניים בישראל.

**הבעיה המרכזית שאנחנו פותרים:**
האתר ממוצב כספק "אתרי תדמית לעסקים קטנים", בזמן שהיכולת האמיתית של הסוכנות היא בניית **מערכות ייצור מורכבות** — פלטפורמת מסחר דו-לשונית לשש סניפים עם ODOO, Stripe, מסכי מטבח וסוויטת Playwright. המיצוב הנוכחי מוריד את ערך העסקה ומושך לידים לא מתאימים.

**המטרה:** להפוך את הפירמידה — מערכות ואוטומציה בראש, אתרי תדמית כמוצר כניסה — ולסגור את פערי האמינות והתשתית הטכנית.

---

## 2. עקרונות עבודה — אלה חוקים, לא המלצות

1. **אל תשנה שום קובץ לפני אישור מפורש ממני.** הצג לי תמיד: מה אתה מתכוון לשנות, באילו קבצים, ולמה. חכה ל־"אשר".
2. **גיבוי לפני שינוי.** לפני עריכה של קובץ קופי או קונפיג — הצג diff מלא, לא תקציר.
3. **שלב אחד בכל פעם.** אל תקפוץ קדימה. בסוף כל שלב: סיכום קצר של מה בוצע + מה נשאר + המתן לאישור.
4. **אל תמציא נתונים.** אם אתה צריך מספר, תאריך, שם לקוח או מדד ביצועים שאין לך — **שאל אותי**. אסור להשלים בניחוש.
5. **קרא לפני שאתה כותב.** לפני שינוי קופי, קרא את הקובץ הקיים ואת המקבילה בשפה השנייה.
6. **עברית ואנגלית נשארות מסונכרנות.** כל שינוי תוכן ב-`/he` מחייב את המקבילה ב-`/en` באותו commit.
7. **אל תדחוף ל-main.** עבוד על ענף `feat/positioning-upgrade` ותן לי לסקור לפני מיזוג.

---

## 3. מחוץ לתחום — אל תיגע בזה

אלה החלטות שכבר קיבלתי. אל תציע אותן, אל תממש אותן, ואל תשאיר עליהן TODO:

- ❌ **תמחור.** אל תוסיף עמוד תמחור, טווחי מחירים, חבילות או מספרים כספיים בשום מקום. אם קיים טקסט מחיר — השאר אותו כמו שהוא.
- ❌ **כתובת המייל.** אל תחליף את כתובת המייל באתר. זה מטופל בנפרד.
- ❌ **עדויות לקוח.** אל תוסיף סקשן testimonials, אל תיצור placeholder לעדויות, ואל תמציא ציטוטים.
- ❌ **תוכן מאמרים לבלוג.** אל תכתוב מאמרים. את התוכן אני כותב בעצמי. (תשתית — כן, ראה שלב 6.)

---

## 4. שלב 0 — מיפוי (קריאה בלבד, אפס שינויים)

לפני הכל, תן לי מפה של הקיים:

- מבנה התיקיות של `app/` — כל הראוטים ב-`/he` וב-`/en`
- איפה יושב הקופי: קבצי i18n / JSON / MDX / hardcoded בקומפוננטות
- איפה מוגדרים ה-metadata (`generateMetadata`, `layout.tsx`, קבצי SEO)
- האם קיים `sitemap.ts` / `robots.ts` / JSON-LD, ואם כן — איפה
- רשימת קומפוננטות ה-hero, השירותים, ה-stats, תיק העבודות והפוטר
- **טבלת פערי שפה:** אילו עמודים/מחרוזות קיימים ב-`/he` וחסרים ב-`/en` (ולהפך)

פלט: טבלה אחת מרוכזת. אל תתקן כלום עדיין.

---

## 5. שלב 1 — תיקוני אמינות (מהיר, סיכון נמוך)

באגי טקסט שנראים חובבניים ללקוח פוטנציאלי:

| מיקום | קיים | לתקן ל |
|---|---|---|
| לוגו טקסטואלי בפוטר | `PRPEL` | `PROPEL` |
| סקשן "אודות" | "בונים, **מאטמטים** ומקדמים" | "בונים מערכות, מייעלים תהליכים ומקדמים בגוגל" |

בנוסף:

- **אחידות גוף.** האתר מתערבב בין "אנחנו" (רוב הקופי) לגוף ראשון יחיד ("ואחזור אליכם עם הצעה קונקרטית" בסוף תיק העבודות). **אחד את הכל ל"אנחנו"** ותקן כל חריגה.
- הרץ סריקת typos על כל מחרוזות העברית באתר והצג לי רשימה לאישור לפני תיקון.

---

## 6. שלב 2 — היפוך המיצוב (הליבה)

### 6.1 סדר השירותים

שנה את סדר שלושת השירותים הראשיים בעמוד הבית מ:
`אתר תדמית → אוטומציה → SEO`
ל:
`מערכות מותאמות → אוטומציה ואינטגרציות → אתרים וקידום`

"מערכות מותאמות" הוא כרטיס **חדש** בראש הרשימה. תוכן מוצע (התאם לניסוח הקיים):

> ### מערכות מותאמות
> כשהאקסל, ה-CRM והקופה כבר לא מדברים — בונים מערכת אחת שהעסק רץ עליה.
> - מערכות הזמנות וניהול רב-סניפיות
> - אינטגרציה למערכות שכבר יש לכם (ERP, קופה, סליקה)
> - מסכי תפעול ייעודיים למטבח, מחסן או צוות שטח
> - נבנה עם בדיקות אוטומטיות — לא "עובד אצלי"
>
> עובדים עם: Next.js · PostgreSQL · Supabase · ODOO · Stripe · Playwright

הכרטיס הקיים "בניית אתר תדמית" יורד למקום השלישי ומתמזג עם SEO, או נשאר עצמאי — הצע לי את שתי האפשרויות עם diff לפני שאתה מחליט.

### 6.2 Hero

הצע לי **שלוש** גרסאות ל-hero במיצוב החדש, ואל תבחר בעצמך. נקודת מוצא לכיוון:

> **המערכת שהעסק שלך רץ עליה — לא רק אתר שנראה טוב**
> אנחנו בונים מערכות ניהול, הזמנות ואוטומציה שמחזיקות עסקים בייצור — ואת האתר שיושב מעליהן.

שמור על אורך דומה לקיים כדי לא לשבור את הלייאאוט.

### 6.3 Metadata

עדכן את ה-title וה-description בהתאם למיצוב החדש, בעברית ובאנגלית.
הימנע מ"אתר תדמית" כמילת מפתח דומיננטית ב-title של עמוד הבית — היא תישאר בעמוד השירות הייעודי.

### 6.4 מספרי ה-Stats

הבלוק הנוכחי (`פרויקטים חיים 5 · עמודים שנבנו 46 · תחומים 3`) מוכר את הסוכנות בזול. "46 עמודים" הוא לא הישג.

**החלף בארבעה מדדים שמעידים על מורכבות הנדסית.** אל תמציא מספרים — קח רק מה שמאומת מהמסמך הזה:

- `6` סניפים פעילים על מערכת אחת
- `38` בדיקות אוטומטיות בסוויטת ה-E2E
- `3` מערכות בשימוש יומיומי בייצור
- `4` אינטגרציות חיצוניות (ODOO, Stripe, Supabase, GA4)

אם אתה רוצה מדד חמישי — **שאל אותי**, אל תחשב לבד.

---

## 7. שלב 3 — תיק עבודות

### 7.1 מבנה אחיד לכל קייס

כל עמוד תיק עבודות חייב לעבור למבנה:
**הבעיה → ההחלטה ההנדסית → התוצאה המדידה**

היום העמודים מתארים *מה נבנה*. הם צריכים לתאר *מה זה פתר*.

### 7.2 J-Cafe — הסיפור החזק ביותר, וכרגע הוא לא באתר

זהו קייס הדגל. כתוב אותו במלואו לפי החומר הבא (אל תוסיף פרטים שלא כתובים כאן):

> **הבעיה:** שלושה מקורות אמת מתחרים לזהות הסניף גרמו להזמנות לדלוף בין סניפים. במערכת היה fallback קשיח לסניף ברירת מחדל בכ-38 מקומות בקוד.
>
> **ההחלטה:** הפיכת השרת לסמכות בלעדית דרך `resolveOrderCompany`, מעבר ל-SSOT מבוסס cookie מסוג httpOnly, מפתחות עגלה נפרדים לכל סניף, והסרת כל ה-fallbacks הקשיחים.
>
> **התוצאה:** סוויטת הבדיקות גדלה מ-7 ל-38 בדיקות. אפס דליפות הזמנות מאז.

זהו הטקסט שממצב את הסוכנות כמהנדסים ולא כבוני-אתרים. תן לו נוכחות ויזואלית בולטת בעמוד.

### 7.3 פרויקטים "מאחורי התחברות"

`BOM & Recipes` ו-`Air Manage` מסומנים כ"פרויקט פרטי — המערכת מאחורי התחברות". מבחינת מבקר, זה כלום.

בנה קומפוננטת `<PrivateProjectShowcase>` שמקבלת מערך מדיה ומציגה גלריה/קרוסלה עם תמיכה בווידאו קצר, plus מצב `placeholder` נקי כשעדיין אין מדיה. **אל תמציא צילומי מסך ואל תייצר תמונות מדומות** — רק את התשתית. אני אספק הקלטות מסך עם דאטה דמה בהמשך.

---

## 8. שלב 4 — SEO טכני

זו הנקודה הכי רגישה: אנחנו **מוכרים** SEO. אם לקוח פוטנציאלי יבדוק את האתר בכלים, הוא חייב להיות נקי.

- [ ] **hreflang** — כרגע יש `canonical` אבל לא ראיתי תגי `alternate` בין `/he` ל-`/en`. הוסף `hreflang` הדדי מלא לכל עמוד, כולל `x-default`.
- [ ] **JSON-LD** — ודא שקיימים ותקפים:
  - `Organization` בלייאאוט הראשי
  - `Service` בכל עמוד שירות
  - `FAQPage` על סקשן השאלות בעמוד הבית
  - `BreadcrumbList` בכל עמוד תיק עבודות ושירות
  - `WebSite` עם `inLanguage` נכון לכל שפה
- [ ] **sitemap.xml** — ודא שכל הראוטים בשתי השפות נכללים, עם `lastModified` אמיתי
- [ ] **robots.txt** — ודא שהוא מצביע ל-sitemap ולא חוסם שום דבר בטעות
- [ ] הרץ ולידציה על ה-JSON-LD והצג לי את התוצאות

---

## 9. שלב 5 — ביצועים, נגישות וטפסים

- [ ] **Lighthouse במובייל בלבד**, עם throttling של 4G. האתר מבטיח "טעינה מתחת לשנייה" — צריך לאמת שאנחנו עומדים בזה. הצג לי LCP, CLS, INP וגודל ה-bundle. **אל תשנה קוד ביצועים לפני שהצגת לי את המספרים.**
- [ ] **שדה ה-honeypot** בטופס יצירת הקשר (`Company website`) מופיע בטקסט המחולץ של העמוד. ודא שהוא `aria-hidden="true"`, `tabindex="-1"`, `autocomplete="off"` — ולא רק `display:none` על ה-wrapper.
- [ ] **נגישות ת"י 5568** — הרץ בדיקת axe על כל העמודים. בדוק במיוחד: ניגודיות צבעים על רקע הקרם, ניווט מקלדת מלא, focus ring נראה, וסדר קריאה נכון ב-RTL. הצג רשימת ליקויים לפני תיקון.
- [ ] **פערי `/en`** — סגור את הפערים שמופו בשלב 0.

---

## 10. שלב 6 — תשתית בלוג (בלי תוכן)

עמוד הבלוג הנוכחי הוא רשימת קישורים חיצוניים בלבד, עם המשפט "מאמרים משלנו יתווספו כאן". זו סתירה גלויה לשירות ה-SEO שאנחנו מוכרים, והעמוד גם שולח תנועה החוצה.

**מה לעשות — תשתית בלבד:**

1. בנה pipeline למאמרים ב-MDX תחת `content/blog/{he,en}/` עם frontmatter: `title`, `description`, `date`, `category`, `slug`, `readingTime`, `draft`.
2. צור ראוט דינמי `/[locale]/blog/[slug]` עם `generateStaticParams` ו-`generateMetadata`.
3. הוסף `Article` JSON-LD + OG image דינמי לכל מאמר.
4. מאמרים עם `draft: true` **לא** מופיעים ב-sitemap ולא ברשימה בפרודקשן.
5. צור קובץ תבנית `content/blog/_TEMPLATE.mdx` עם ה-frontmatter מוכן ושלד כותרות — אני אמלא אותו.
6. הקישורים החיצוניים הקיימים עוברים לסקשן משני בתחתית העמוד בשם "מקורות שאנחנו חוזרים אליהם", עם `rel="noopener"`. הם לא התוכן הראשי.

**אל תכתוב מאמרים.** אפילו לא טיוטה, אפילו לא ראשי פרקים של מאמר ספציפי.

---

## 11. שלב 7 — עמודים חדשים

הצע לי מבנה (לא תוכן מלא) ל:

1. **עמודי אנכיים** — עמודי נחיתה לפי סוג עסק, שכל אחד מגובה בקייס אמיתי שכבר קיים:
   - מערכת לרשת מסעדות / בתי קפה (מגובה ב-J-Cafe)
   - אתר לקליניקה ומכון טיפולי (מגובה בכנפיים לעוף)
   - אתר לעסק שירות ותפעול שטח (מגובה בהגורר 2)
2. **עמוד "מתי אנחנו לא הכתובת"** — מחזק את ערך השקיפות שכבר מוצהר באתר ומסנן לידים לא מתאימים. **בלי מספרים כספיים** — הסינון הוא לפי סוג פרויקט, לא לפי תקציב.
3. **הורדת חיכוך ביצירת קשר** — בדוק היתכנות לשילוב Cal.com לתיאום שיחת אפיון במקום "נחזור אליכם". הצג לי את העלות בביצועים לפני מימוש.

---

## 12. קריטריוני קבלה

השלמת המשימה נמדדת כך:

- [ ] אפס טעויות כתיב בעברית באתר
- [ ] "מערכות" מופיע לפני "אתר תדמית" בכל היררכיית העמוד הראשי, כולל ה-metadata
- [ ] כל בלוק ה-stats מבוסס על נתונים מהמסמך הזה בלבד
- [ ] hreflang הדדי תקין בכל עמוד, מאומת
- [ ] JSON-LD עובר ולידציה ללא שגיאות
- [ ] Lighthouse מובייל: Performance ≥ 95, Accessibility = 100, SEO = 100
- [ ] `/en` ו-`/he` בפריטי תוכן זהים — אפס עמודים יתומים
- [ ] axe: אפס ליקויים ברמת violation
- [ ] תשתית הבלוג עובדת מקצה לקצה עם קובץ MDX דמה, **ואפס מאמרים אמיתיים נכתבו**
- [ ] אפס אזכורי מחיר חדשים בקוד
- [ ] כתובת המייל לא שונתה

---

## 13. תחילת עבודה

התחל **בשלב 0 בלבד**. הצג לי את המיפוי ואת טבלת פערי השפה, ואז עצור והמתן.
